<?php

namespace App\Http\Controllers\Pract;

use App\Traits\Utils;

class DataStructuresController extends Controller {
    use Utils;

    /**
     * implement linear search algorithms
     * @param array $array the array where we are going to search an element
     * @param int $search the element has be searched
     * @return int if the element if found, returns the index, -1 otherwise
     */
    public function linearSearch(array $array, int $search) : int {

    }



}